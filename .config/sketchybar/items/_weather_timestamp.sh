WEATHER_TIMESTAMP=(
  --add bracket _weather_timestamp _weather _timestamp
  --set _weather_timestamp
  drawing=on
  background.drawing=on
  background.corner_radius=4
  background.border_width=0
  background.y_offset=0
)

sketchybar "${WEATHER_TIMESTAMP[@]}"