# Disabled Neovim plugin configs

These plugin specs are archived while the active Neovim setup stays plain.

Current active setup:

- Neovim core config
- `lazy.nvim` bootstrap/plugin manager
- Empty plugin skeletons:
  - `home/.config/nvim/lua/config/plugins/init.lua`
  - `home/.config/nvim/lua/config/plugins/lsp/init.lua`

To re-enable a plugin later, copy or move its Lua file back to:

```text
home/.config/nvim/lua/config/plugins/
```

For LSP-related plugins, use:

```text
home/.config/nvim/lua/config/plugins/lsp/
```
